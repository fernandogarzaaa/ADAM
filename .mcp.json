{
  "$comment": "Drop-in MCP server config for Claude Code and any client that reads .mcp.json. Run `cargo build --release` in this repository once before use — the path below resolves relative to the repository root. Set ADAM_MEMORY_PATH to control where the organism's memory database lives; it defaults to the process working directory.",
  "mcpServers": {
    "adam": {
      "command": "./target/release/adam-mcp",
      "args": []
    }
  }
}
